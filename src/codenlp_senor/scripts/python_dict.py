# Initialize a dictionary
country_symbols = {}

# Add key-value:
country_symbols["en"] = "English"

# Check for key presence
is_present = "en" in country_symbols

# Get the value for the key
value = country_symbols["en"]
